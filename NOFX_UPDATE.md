# NOFX 更新内容

本文档汇总近期 NOFX 系统在策略预设、持仓/交易展示、回测与实盘一致性等方面的更新。

---

## 策略与提示词（版本 3.0）

### 策略预设
- **默认策略版本** 定为 **3.0**（`StrategyConfig.Version = "3.0"`）。
- **止盈止损预设**（更不保守）：
  - 初始止损：**4%**
  - 分层止盈：**4% / 7% / 10%** 三档，对应平仓比例可配置。
  - ATR 倍数区间、ATR 周期等按币种/市场在策略中可配置，提示词中说明由 AI 根据行情与币种在区间内判断。
- **最小持仓时间**：策略支持最小持仓（如 5 分钟），前端策略配置中需展示该项并作为预设的一部分。
- **风险收益比**：最小 3:1（AI 指导区间内）。

### 提示词同步
- **System Prompt 自定义** 与 **提示词** 已与上述预设一致：
  - 角色定义为「优化版 v3.0」。
  - 明确说明 ATR 倍数区间、ATR 周期为动态区间，由 AI 根据币种与市场状况在区间内选择。
  - 止盈止损、分层止盈、追踪止损等规则与策略参数一致。
- 默认策略与优化策略均从 `GetDefaultStrategyConfig` / `GetOptimizedStrategyConfig` 读取，注释与预设值已统一。

---

## 当前持仓板块

### 展示原则
- **价格、金额、数量** 使用 **完整实际数值**，不四舍五入（前端通过 `formatFull` 等实现）。
- 不显示「固定」「实时」标题，仅通过两块内容区分。

### 第一块：参数（中间区域）
- 止盈价、止损价、初始止损位  
- ATR **倍数**（不再仅显示 ATR 数值）、ATR 数值、ATR 周期（若有）  
- 布局：网格/分组、清晰字体（如 `font-mono`），可与回测当前持仓风格一致。

### 第二块：实时状态（单独区域）
- 距止损、距止盈  
- 追踪止损：是否激活、达到哪一层、允许回撤多少  
- 分层止盈：是否激活、达到哪一层、已平仓比例  
- 当前盈亏  
- 不显示「实时」标题，仅以区块区分。

### 实盘与回测一致
- 实盘（TraderDashboardPage）与回测（BacktestPage）的当前持仓在 **字段种类、两块结构、完整精度** 上保持一致。
- 若实盘 API 返回 `trailing_tier_activated`、`trailing_allowed_drawdown`、`scaled_tp_closed_pct`、`atr_period` 等，前端已支持展示。

---

## 交易记录（开仓/平仓）

### 开仓记录
- 展示与当前持仓 **同类型** 的参数：止损价、止盈价、初始止损位、ATR 倍数/数值/周期等。
- 数值使用完整精度，不四舍五入。

### 平仓记录（最终信息）
- 平仓后的交易记录与 **当时持仓** 的参数种类一致。
- **实时的参数在交易完成后变为最后的固定值**，例如：
  - 平仓原因（close_reason）
  - 追踪止损：最终激活档位、允许回撤
  - 分层止盈：最终档位、已平仓比例
  - 已实现盈亏
- 后端（回测）在平仓事件上写入并返回：`stop_loss`、`take_profit`、`atr_at_open`、`atr_multiple_sl/tp`、`atr_period`、`scaled_tp_level`、`scaled_tp_closed_pct`、`trailing_tier_activated`、`trailing_allowed_drawdown` 等，供前端展示为「最终信息」。

### 实盘与回测一致
- 交易列表中的参数种类与当前持仓一致；平仓记录展示的是 **最终固定值**。
- 实盘交易记录与回测在展示逻辑上对齐（字段与布局一致）。

---

## 回测实验室

### 回测当前持仓
- 与上述「当前持仓」设计一致：两块结构、完整精度、无「固定」「实时」标题。
- 展示：止损价、止盈价、初始止损位、ATR 倍数/数值/周期；以及距止损/止盈、追踪档位、允许回撤、分层止盈档位/已平仓%、当前盈亏等。

### 回测交易列表
- 每笔交易（开仓/平仓）使用与当前持仓 **相同的参数种类**。
- 平仓卡片展示 **平仓时刻的最终值**（同上），并支持 AI 分析（出场质量、风险管理等）。

### 动态止盈/止损执行
- 回测与实盘使用同一套策略逻辑：动态止损（含追踪分层）、动态止盈（含分层止盈）在回测中同样执行。
- 策略触发平仓时，会在交易事件中记录 `close_reason`，并写入上述最终参数字段，便于 AI 分析与前端展示。

### AI 分析
- 回测支持「启用交易 AI 分析」：平仓后可对单笔交易进行 AI 分析（出场质量、市场环境、风险管理、改进建议等）。
- 前端提供 **AI 分析** 板块/标签页，单独展示 AI 分析结果与统计。

---

## 前端与后端

### 前端
- **完整精度**：价格、数量、盈亏等通过 `formatFull`（如 `web/src/utils/format.ts`）展示，避免 toFixed 造成的信息丢失。
- **当前持仓**：回测页与实盘页均为「参数块 + 实时块」两段式，风格统一。
- **交易记录**：开仓/平仓卡片与当前持仓参数种类一致，平仓为最终值；实盘与回测展示逻辑一致。
- **策略配置**：前端策略页需展示最小持仓时间等全部预设项，并与后端预设一致。

### 后端
- **回测 TradeEvent**：平仓事件包含 `stop_loss`、`take_profit`、`atr_at_open` 以及 `atr_multiple_sl`、`atr_multiple_tp`、`atr_period`、`scaled_tp_level`、`scaled_tp_closed_pct`、`trailing_tier_activated`、`trailing_allowed_drawdown` 等最终快照字段。
- **回测 StatusPayload**：当前持仓状态包含与前端两块对应的字段，供回测页与实盘页共用同一套展示逻辑。
- **实盘**：若实盘 API 返回与回测相同的实时/最终字段，前端已预留展示支持。

---

## 其他

- **Prompt 缓存**：若使用支持缓存的模型与分组（如 Claude Default-cache），前端可展示「Prompt 缓存：读取 / 创建」统计（依赖 API 返回的 usage 信息）。
- **Docker 部署**：通过 `docker compose up -d --build` 可完成本机部署，前端默认 3000、后端默认 8080，需配置 `.env`（如 JWT_SECRET、DATA_ENCRYPTION_KEY、RSA 密钥等）。

---

*文档根据当前代码与需求整理，具体以实际发布版本为准。*
